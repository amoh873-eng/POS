# .NET
bin/
obj/
*.user
*.suo
.vs/
# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
pubspec.lock
build/
*.g.dart
*.freezed.dart
# DB
*.db
*.sqlite
*.sqlite3
# Env
.env
secrets.json
# Note: appsettings.Development.json IS committed (safe dev defaults); only .env holds secrets
# appsettings.Development.json — keep committed for local DX (contains DEV_ONLY keys)
# IDE
.idea/
*.code-workspace
# OS
Thumbs.db
.DS_Store
# Extracted temp
_extracted.txt
_doc_extracted.txt
# Local discovery dumps (never commit)
pos.txt
project-files.txt
# Flutter SDK clone (do not commit)
flutter/
